Plot Waves:

Takes a value n, and plots the functions sin(x), sin(2*x), ..., sin(n*x) on a graph
Then plots sin(n*x) with a sample frequency of 10hz, with samples quantized to 2 bits on the same graph
